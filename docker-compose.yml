services:
  app:
    build: .
    container_name: southwest-checkin
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    shm_size: "1gb"
