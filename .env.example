# Copy this file to .env and fill in your own values.
# .env is gitignored so your secrets stay on your machine.

# Username you'll type at http://localhost:3000/login
AUTH_USERNAME=changeme

# Password you'll type at http://localhost:3000/login
AUTH_PASSWORD=changeme

# A long random string used to sign login cookies.
# Generate one with:  openssl rand -hex 32
AUTH_SECRET=replace-with-a-long-random-string
